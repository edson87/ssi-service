spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dhmysql
    username: root
    password: admin
  jpa:
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: true